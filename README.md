# URL SHA-256 Checker

A simple Python program to calculate the SHA-256 hash of a URL.  
Useful for file integrity verification or certificate validation.

## Usage

1. Run the script:

```bash
python SHAvalueForURL.py
